extends Label

@onready var mapper: MapperRoot = $"../../../../../.."

# Called when the node enters the scene tree for the first time.
#func _ready() -> void:
#	mapper.base_zoom.value_changed.connect(_zoom)

#func _zoom(val: ReactiveFloat) -> void:
#	text = "zoom: %d%%" % (val.value * 100)
